{
  "name": "statslab-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "StatsLab: Dasbor Statistika Interaktif & EdTech R&D Ecosystem",
  "scripts": {
    "dev": "pnpm -F web dev",
    "build": "pnpm -r build",
    "postinstall": "pnpm --filter web run postinstall",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@10.4.1"
}
